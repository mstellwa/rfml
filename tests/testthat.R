# library(testthat)
# library(rfml)
#
# #.get.param.inputs(c(".host",".port", ".username", ".password"))
#
# ml.connect(host = .host, port = .port, username = .username, password = .password)
# #ml.init.database(host = .host, port = .port, username = .username, password = .password)
# test_check("rfml")
